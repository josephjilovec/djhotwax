"use client";
import { FormEvent, useState } from "react";
export function ContactForm(){const[s,setS]=useState<"idle"|"sending"|"sent"|"error">("idle");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setS("sending");const form=e.currentTarget;const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(!r.ok)throw new Error();form.reset();setS("sent")}catch{setS("error")}}return <form className="contact-form" onSubmit={submit}><label><span>Name</span><input name="name" required/></label><label><span>Email</span><input name="email" type="email" required/></label><label><span>Inquiry</span><select name="type" defaultValue="Licensing"><option>Licensing</option><option>Production collaboration</option><option>Placement</option><option>Creative partnership</option><option>General</option></select></label><label><span>Message</span><textarea name="message" rows={5} required/></label><button disabled={s==="sending"}>{s==="sending"?"Sending…":"Start a conversation"}<span>↗</span></button><p className={`form-status ${s}`} aria-live="polite">{s==="sent"?"Message received.":s==="error"?"Could not send. Email realjjemail@gmail.com.":"Licensing · placements · collaborations"}</p></form>}
