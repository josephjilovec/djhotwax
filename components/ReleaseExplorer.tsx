"use client";
import { useState } from "react";
import { releases } from "@/lib/releases";
export function ReleaseExplorer(){const[active,setActive]=useState(releases[0].id);const current=releases.find(r=>r.id===active)??releases[0];return <div className="release-explorer"><div className="release-list">{releases.map((r,i)=><button key={r.id} onClick={()=>setActive(r.id)} className={r.id===active?"active":""}><span>{String(i+1).padStart(2,"0")}</span><strong>{r.title}</strong><small>{r.category}</small></button>)}</div><div className="release-stage"><div className="release-stage-image"><img key={current.cover} src={current.cover} alt={`${current.title} artwork`}/><a href={current.url} target="_blank" rel="noreferrer" aria-label={`Play ${current.title} on YouTube`}>▶</a></div><div className="release-stage-copy"><div><span>{current.world}</span><span>{current.year}</span></div><h3>{current.title}</h3><p>{current.description}</p><a className="text-link" href={current.url} target="_blank" rel="noreferrer">Play on YouTube ↗</a></div></div></div>}
