"use client";
import { useState } from "react";
import { releases } from "@/lib/releases";
export function TurntableHero(){const[playing,setPlaying]=useState(true);const r=releases[0];return <div className={`turntable-hero ${playing?"playing":""}`}><div className="ambient a"/><div className="ambient b"/><button className="record-button" onClick={()=>setPlaying(v=>!v)} aria-label={playing?"Pause visualizer":"Play visualizer"}><img className="record-cover" src={r.cover} alt={`${r.title} cover art`}/><span className="record-ring"/><span className="record-center">HOT<br/>WAX</span></button><div className="tone-arm"><i/><b/></div><div className="equalizer" aria-hidden="true">{Array.from({length:28},(_,i)=><i key={i} style={{animationDelay:`${(i%7)*.08}s`}}/>)}</div><div className="deck-label"><span>NOW SPINNING</span><strong>{r.title}</strong><small>{playing?"VISUALIZER LIVE":"PAUSED"}</small></div></div>}
